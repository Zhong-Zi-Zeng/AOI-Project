_base_: [ "./base/model/EfficientAD/efficient_ad_base.yaml", # model
          "./base/evaluation/object_detection.yaml", # task
          "./base/dataset.yaml", # dataset
          "./base/hyp.yaml" # hyperparameter
]

# ===========Dataset===========
coco_root: "./data/coco/original_class_1"  # inference：efficient_ad 只會辨識一種
dataset_dir: './data/mvtec/original_class_4'  # inference：efficient_ad 只會辨識一種
number_of_class: 4  # inference：efficient_ad 只會辨識一種
class_names:  # inference：efficient_ad 只會辨識一種
  - "Scratch"
  - "Friction"
  - "Dirty"
  - "Assembly"

# ===========Hyperparameter===========
# optimizer: "SGD" # AdamW、SGD
weight: "./work_dirs/model-v1.ckpt" # Used when inference
start_epoch: 0
end_epoch: 50
#warmup_epoch: 3
#initial_lr: 0.0003 # Initial learning rate
lr: 0.001 # The learning rate after warmup
#minimum_lr: 0.0001 # Minimum learning rate
batch_size: 4
imgsz: [1024, 1024]  # Used when inference
#use_patch: false
device: 0

